general {
  colors = true
    interval = 1
    separator = ""
}

order += "disk /"
order += "tztime local"

tztime local {
  format = " %d/%m/%Y | 󰥔 %H:%M:%S"
}

load {
  format = "%1min"
}

disk "/" {
  format = " %avail |"
}
